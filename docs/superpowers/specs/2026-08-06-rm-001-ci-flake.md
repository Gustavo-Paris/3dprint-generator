# rm-001 — Stabilize find-or-create-user CI flake

- **Roadmap:** rm-001
- **Date:** 2026-08-06

## Loop contract

- **spec:** The integration suite no longer asserts that a naive check-then-insert race *must* reject under concurrency (that assertion is load-dependent and was red on GHA). Production `findOrCreateUser` stays ON CONFLICT; the concurrent-safe test remains the gate.
- **done:** `pnpm exec vitest run tests/integration/find-or-create-user.test.ts` green; no test expects a forced UNIQUE collision.
- **don't:** Do not skip or weaken the concurrent `findOrCreateUser` success test; do not change auth product behavior beyond the test.
