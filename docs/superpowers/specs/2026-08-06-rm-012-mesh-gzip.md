# rm-012 — Mesh transfer gzip

- **Roadmap:** rm-012
- **Date:** 2026-08-06

## Loop contract

- **spec:** GET /meshes/[file] gzips responses when Accept-Encoding includes gzip and payload ≥ 2KB. 3MF multi-color serialize already runs in a worker (prior work).
- **done:** Code path + unit suite still green; manual: Accept-Encoding: gzip returns content-encoding.
- **don't:** Do not break auth/ownership gates on the mesh route.
