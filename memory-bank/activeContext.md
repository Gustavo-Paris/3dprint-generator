# Active Context


## Notes

- **2026-05-18** — Sessão 2026-05-17 fechou com 3 composers determinísticos funcionando (chaveiro/porta-copo/medalha) + universal Meshy+logo composer + size parser. Medalha imprimiu 6 unidades na Bambu. Próximos passos no sprint: CHORE-001 (commit), depois FEAT-001/002/003 (pingente, ímã, plaquinha), depois CHORE-002 (cleanup órfão). Postgres voltou após restart do Docker. Dev server pid 82984.
- **2026-05-18** — Refactor done: detect-X/composer-X ladder → 1 LLM parser + 1 parametric generator + Meshy freeform fallback. Added: download STL button, design JSON inline editing (bypass LLM), sanity clamps with adjustments surfaced in chat. 49 unit tests passing. Files: src/lib/design/{schema,parse,generate,sanitize}.ts. 21 legacy files deleted.
- **2026-05-19** — Meshy removed entirely. Schema: 3 primitives only (hollow_cylinder/flat_plate/disc), no freeform. LLM biased toward closest-primitive fallback for exotic shapes (trophy → tall flat_plate, etc.). Deleted: src/lib/meshy/, with-meshy-base.ts, meshy tests, MESHY_API_KEY from env. 46 tests passing.
- **2026-05-19** — FEAT-010+011: hangingHole.position field (top/top_left/top_right/left/right/bottom) + deterministic quick-modifier layer that catches common iteration phrases ('logo maior', 'tira a alça', 'buraco do lado direito', 'mais alto') and applies the modification without calling the LLM. Order: designOverride → quickModify → LLM. Quick modifier covers ~80% of routine iterations reliably, LLM handles complex multi-field changes. 64 unit tests.
- **2026-05-19** — FEAT-012+013: flat_plate now LIES FLAT (Z=thickness, X=width, Y=height) for print-ready orientation. Hole drilled along Z axis perpendicular to widest face. Logo placed on +Z (top face) via (x,y,z)->(x,z,y) reflection + winding reversal. standAngleDeg is now a sizing hint only, ignored at build (plaquinha prints flat too). FEAT-013: logo sizing uses sharp metadata to read image aspect ratio, scales to fill the larger plate dim when image is wider/taller than plate's min dim. 65 tests passing.
- **2026-05-19** — FEAT-014: composite primitive (stack 2-4 primitives via offsetZ) + flat_plate.orientation: 'flat'|'vertical'. Trophy = disc base + vertical flat_plate top. Composite generator builds each part recursively, applies offsetZ translation, concatenates triangles. Quick-modifier bails on composites (LLM handles ambiguity). 68 tests passing.
- **2026-05-19** — BUG-001: ignoreHolesSmallerThan=0.3 was filtering legitimate inner-channel holes of outlined logos (PG monogram showed as silhouette instead of line-art). Changed all 4 callsites in generate.ts to 0 (preserve all holes; potrace turdSize=8 still filters pixel noise). 68 tests still pass.
- **2026-05-19** — BUG-002: schema validation was rejecting LLM outputs that overshoot caps (sizeRatio>0.95, standAngleDeg>80) — broke iteration when user said 'aumenta mais' past the cap. Switched .max() to .transform() so values clamp silently. Also expanded quick-modifier regex to catch 'aumenta mais', 'ainda maior', 'bem maior', 'muito maior', bare 'maior'/'menor'. 78 tests passing.
- **2026-05-19** — BUG-003: extrudeLogo defaults too aggressive for line-art logos. blur 0.8→0.3 (preserves 3px strokes), turdSize 8→2 (preserves thin curved segments), optTolerance 0.2→0.1 (preserves subtle bends). PG monogram should trace cleanly now without fragments.
- **2026-05-19** — BUG-004: pipeline failed on gradient-coloured logos (PG monogram traced as bbox blob because Otsu couldn't separate light-gradient pixels from white). Added sharp.normalize() after grayscale to stretch the histogram before threshold. Walked back BUG-003 over-tuning: blur 0.3→0.4, turdSize 2→4, optTolerance 0.1→0.15. Should restore stroke fidelity without halo artifacts.
- **2026-05-19** — BUG-005: gradient logos kept failing the trace because Otsu picked permissive thresholds on smudged anti-aliased edges. Pre-binarize with sharp.threshold(200) before potrace — guarantees clean B/W bitmap input, no Otsu surprises. Pipeline: flatten→grayscale→normalize→blur(0.4)→threshold(200)→potrace.
- **2026-05-19** — Added binaryThreshold knob to LogoSpec + extrudeLogo (default 230). Pre-binarises before potrace. User can override via 'Editar JSON' if 230 doesn't capture their specific logo's darkness range.
- **2026-05-19** — Reverted trace params to original safe defaults: blur 0.8, turdSize 8, optTolerance 0.2. Only kept: sharp.normalize() (helps gradient logos), ignoreHolesSmallerThan=0 (preserves line detail when trace works), and binaryThreshold opt-in knob. Tracing gradient logos like the PG monogram is fundamentally tricky — recommended user workflow now is: try default first, then 'Editar JSON' and add 'binaryThreshold': 200/220/240 until logo reads clean.
- **2026-05-19** — FEAT-015: THE actual fix for gradient-logo tracing — use the PNG's alpha channel directly as the ink mask. Designer-encoded transparency IS the perfect ink/background discrimination; no need to threshold colors. Fast path: hasAlpha + alphaMin<240 → extract alpha → negate → potrace. Slow path: RGB+normalize+blur (for JPGs and flat PNGs). Sidesteps every Otsu/threshold/contrast issue we'd been chasing.
- **2026-05-19** — FEAT-016: new 'channels' treatment. Engraves ONLY the inner holes (channels) of each letter outline, leaving strokes at plate level. Implementation: extrudeLogo.mode='channels_only' extrudes per-hole instead of per-(outer-holes). Visual effect: low-relief logo where letters appear raised because surrounding channels are recessed. Triggered by 'baixo relevo' / 'só os canais cavados' in user message.
- **2026-05-19** — Fixed channels_only mode: now subtracts directly-nested outer strokes from each hole before carving. For parallel-stroke letters (depth 0/1/2/3 nesting), carves: depth-1 minus depth-2 (the channel between outer and inner strokes) + depth-3 (center). Both strokes survive at plate level.
- **2026-05-19** — CHORE-004: removed 'channels' treatment from user-facing schema after honest assessment — fragile for outlined logos with complex nesting, couldn't converge without preview UI. Internal mode='channels_only' infrastructure stays in extrudeLogo for possible future use. Active treatments: through_cut (works perfectly with alpha pipeline from FEAT-015), engraved, embossed.
- **2026-05-20** — BUG-006: keychain plate was 80×35 (wide) for a square PG logo → logo looked small and lost. Now the route computes the logo image's aspect ratio via sharp and passes it to parseDesign; the LLM system prompt requires plate width:height to match the logo aspect (square logo → square plate). Buffer load moved before parseDesign so aspect is available.
- **2026-05-20** — BUG-007: high-fidelity trace on the alpha path. The alpha mask is a clean designer-authored silhouette, so we can afford aggressive fidelity: turdSize 8→2, optTolerance 0.2→0.1, plus 2x upscale before potrace (more curve-fitting samples → smoother strokes). RGB path stays conservative. Aims to make traced PG monogram match the source more faithfully.
- **2026-05-20** — BUG-008: reverted BUG-007. The 2x upscale + aggressive trace params (turdSize 2, optTolerance 0.1) backfired — upscaling a raster source adds no real detail, just interpolation blur, which potrace traces with extra wobble. Restored exact #69 state: alpha path at native resolution, turdSize 8, optTolerance 0.2. Lesson: don't 'enhance' a raster trace pipeline past the working baseline.
- **2026-05-24** — FEAT-004 + FEAT-005: Added Bookmark (marca-página) and Pin/Botão shapes to the design schema and parametric generator. Implemented bottom-face logo placement support (`bottom_face`) for flat plates and discs, allowing support-free lapel pins to print flat with engraved logos on the bottom. Added sanitization bounds, LLM prompt rules/triggers, quick-modifier scaling support, and unit tests. All 84 unit and integration tests passing.
